{ "name": "quackserver",
  "version": "1.0.0",
  "description": "server for quack app",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "start-dev": "./node_modules/.bin/dotenvx run -- node server.js"
  },
  "author": "shauvia",
  "dependencies": {
    "body-parser": "^2.2.0",
    "cors": "^2.8.5",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.1"
  }
}

